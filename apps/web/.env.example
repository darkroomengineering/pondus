# GitHub API Token (for leaderboard data)
# Create at: https://github.com/settings/tokens
# Scope needed: public_repo (read-only)
# Without this, you're limited to 60 requests/hour (will get 403 errors)
# With token: 5,000 requests/hour
GITHUB_TOKEN=ghp_your_token_here

# Polar.sh Configuration
# Get these from https://polar.sh/settings/developers
POLAR_ACCESS_TOKEN=polar_oat_
POLAR_WEBHOOK_SECRET=polar_whs_

# App URL (used for success redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Master license key (for team use, never expires)
# Generate a secure random string: openssl rand -hex 32
MASTER_LICENSE_KEY=

# Polar Product IDs (create these in Polar dashboard)
# POLAR_PRODUCT_PRO_MONTHLY=prod_xxx
# POLAR_PRODUCT_PRO_YEARLY=prod_xxx

# Turso Database
# Create at: https://turso.tech/app/databases
# 1. Create a database: turso db create specto
# 2. Get URL: turso db show specto --url
# 3. Get token: turso db tokens create specto
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your-auth-token
